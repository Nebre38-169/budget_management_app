import { DateToMonthPipe } from './date-to-month.pipe';

describe('DateToMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new DateToMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
